# -*- coding: utf-8 -*-
#
# mvme documentation build configuration file, created by
# sphinx-quickstart on Fri Jun  9 14:45:22 2017.
#
# This file is execfile()d with the current directory set to its
# containing dir.
#
# Note that not all possible configuration values are present in this
# autogenerated file.
#
# All configuration values have a default; values that are commented out
# serve to show the default.

# If extensions (or modules to document with autodoc) are in another directory,
# add these directories to sys.path here. If the directory is relative to the
# documentation root, use os.path.abspath to make it absolute, like shown here.
import datetime
import os
import sys
print(sys.argv)
sys.path.insert(0, os.path.abspath('../../ext'))


# -- General configuration ------------------------------------------------

# If your documentation needs a minimal Sphinx version, state it here.
#
# needs_sphinx = '1.0'

# Add any Sphinx extension module names here, as strings. They can be
# extensions coming with Sphinx (named 'sphinx.ext.*') or your custom
# ones.
# Add 'rst2pdf.pdfbuilder' to enable the direct pdf builder
# 'autoimage' is a local extension stored in the ext subdirectory.
extensions = ['autoimage', 'sphinx.ext.imgmath']

# Add any paths that contain templates here, relative to this directory.
templates_path = ['_templates']

# The suffix(es) of source filenames.
# You can specify multiple suffix as a list of string:
#
# source_suffix = ['.rst', '.md']
source_suffix = '.rst'

# The master toctree document.
master_doc = 'index'

# General information about the project.
project = u'mvme'
copyright = u'2016-'+ datetime.date.today().strftime("%Y")+u', mesytec GmbH & Co. KG'
#author = u'Florian Lüke <f.lueke@mesytec.com>'
author = u'mesytec GmbH & Co. KG <info@mesytec.com>'

# The version info for the project you're documenting, acts as replacement for
# |version| and |release|, also used in various other places throughout the
# built documents.
#
# The short X.Y version.
#version = u'1.0'
version = u'@GIT_VERSION_TAG@'
# The full version, including alpha/beta/rc tags.
#release = u'1.0'
release = u'@GIT_VERSION_SHORT@'

# The language for content autogenerated by Sphinx. Refer to documentation
# for a list of supported languages.
#
# This is also used if you do content translation via gettext catalogs.
# Usually you set "language" from the command line for these cases.
language = "en"

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
# This patterns also effect to html_static_path and html_extra_path
exclude_patterns = []

# The name of the Pygments (syntax highlighting) style to use.
pygments_style = 'sphinx'

# If true, `todo` and `todoList` produce output, else they produce nothing.
todo_include_todos = False

# If true, figures, tables and code-blocks are automatically numbered if they
# have a caption. At same time, the numref role is enabled. For now, it works
# only with the HTML builder and LaTeX builder. Default is False.
# Note: The LaTeX builder always assigns numbers whether this option is enabled
# or not.
numfig = True

# ### Variables added by autoimage / autofigure
#
# autoimage/autofigure look for images in this directory.
#image_dir = ''

# -- Options for HTML output ----------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
#
html_theme = 'alabaster'

# Theme options are theme-specific and customize the look and feel of a theme
# further.  For a list of options available for each theme, see the
# documentation.
#
# See http://alabaster.readthedocs.io/en/latest/customization.html for
# alabaster theme options.
html_theme_options = {
        'page_width': '90%', # Default: '940px'
        'show_related': True,
        'fixed_sidebar': False,
        #'logo': 'mesytec-logo-yellow.png',
        #'logo_name': True
        }

# Add any paths that contain custom static files (such as style sheets) here,
# relative to this directory. They are copied after the builtin static files,
# so a file named "default.css" will overwrite the builtin "default.css".
#html_static_path = ['_static']


# -- Options for HTMLHelp output ------------------------------------------

# Output file base name for HTML help builder.
htmlhelp_basename = 'mvmedoc'


# -- Options for LaTeX output ---------------------------------------------

latex_elements = {
    # The paper size ('letterpaper' or 'a4paper').
    #
    # 'papersize': 'letterpaper',
    'papersize': 'a4paper',

    # The font size ('10pt', '11pt' or '12pt').
    #
    # 'pointsize': '10pt',

    'classoptions': ',openany,oneside',

    # Additional stuff for the LaTeX preamble.
    #
    # 'preamble': '',

    'preamble': r'''
% lastpage is used to get the total number of pages
\usepackage{lastpage}
%\usepackage[head=36.16432pt,foot=12.4]{geometry}

\setlength{\headheight}{36.16432pt}
\setlength{\footskip}{12.4pt}

\newcommand{\mesyteccontact}{
    \tiny{\textsf{mesytec GmbH \& Co. KG\\
    Wernher-von-Braun-Str.1, D-85640 Putzbrunn, Germany\\
    phone: +49 - 89 / 456007-30 fax: +49 - 89 / 456007-39\\
    info@mesytec.com - www.mesytec.com\\}}}

\newcommand{\mesyteclogo}{\includegraphics[width=2.5cm]{mesytec-logo-yellow.png}}

\makeatletter
\fancypagestyle{normal}{
    \fancyhf{}

    \fancyhead[L]{\mesyteclogo}
    \fancyhead[C]{\@title}

    \fancyfoot[C]{\mesyteccontact}
    \fancyfoot[R]{\thepage/\pageref{LastPage}}

    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0.4pt}
}

% Update the plain style so we get the page number & footer line,
% but not a chapter or section title.  This is to keep the first
% page of a chapter and the blank page between chapters `clean.'
\fancypagestyle{plain}{
    \fancyhf{}

    \fancyhead[L]{\mesyteclogo}
    \fancyhead[C]{\@title}

    \fancyfoot[C]{\mesyteccontact}
    \fancyfoot[R]{\thepage/\pageref{LastPage}}

    \renewcommand{\headrulewidth}{0.0pt}
    \renewcommand{\footrulewidth}{0.4pt}
}
\makeatother

% Use \pagestyle{normal} as the primary pagestyle for text.
\pagestyle{normal}
\thispagestyle{plain}
''',

    # Latex figure (float) alignment
    #
    # Mnemonics: (h)ere, (t)op, (b)ottom, (p)age
    # 'H' disables floating and positions figure where it appears in the source text.
    #
    # 'figure_align': 'htbp',
    'figure_align': 'H',

    # Use the babel package to generate latex indices
    'babel': '\\usepackage{babel}',
}

# Grouping the document tree into LaTeX files. List of tuples
# (source start file, target name, title,
#  author, documentclass [howto, manual, or own class]).
latex_documents = [
    (master_doc, 'mvme.tex', u'mvme - mesytec VME Data Acquisition\\\\',
     u'mesytec GmbH \\& Co. KG \\textless{}info@mesytec.com\\textgreater{}',
     'manual'),
]

#latex_logo = 'images/mesytec-logo-yellow.png'

latex_additional_files = [
        'mesytec-logo-yellow.png'
        ]

# -- Options for rst2pdf output -------------------------------------------
# Tuples of the form
# (master document, pdf name, title, author)
pdf_documents = [
    ('index', 'mvme', u'mvme - mesytec VME Data Acquisition',
     u'Florian Lüke <f.lueke@mesytec.com>'),
]

# -- Options for manual page output ---------------------------------------

# One entry per manual page. List of tuples
# (source start file, name, description, authors, manual section).
man_pages = [
    (master_doc, 'mvme', u'mvme Documentation',
     [author], 1)
]


# -- Options for Texinfo output -------------------------------------------

# Grouping the document tree into Texinfo files. List of tuples
# (source start file, target name, title, author,
#  dir menu entry, description, category)
texinfo_documents = [
    (master_doc, 'mvme', u'mvme Documentation',
     author, 'mvme', 'mesytec VME Data Acquisition',
     'Miscellaneous'),
]

# -- Options for QtHelp output --------------------------------------------
qthelp_namespace = "com.mesytec.mvme." + release
qthelp_theme = 'alabaster' # default: 'nonav'
qthelp_theme_options = { 'nosidebar': True }
